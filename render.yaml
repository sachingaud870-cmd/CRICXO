services:
  - type: web
    name: cricxo
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python manage.py migrate && python manage.py collectstatic --noinput
    startCommand: gunicorn cricxo.wsgi
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.5
